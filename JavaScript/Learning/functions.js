// Functions is a collection of statements that are used in the application to perform a specific task.  The functions are called from the main

function addNumbers(a, b) {
    return a + b;
}

var result1 = addNumbers(2, 3);
var result2 = addNumbers(4, 5);

console.log(result1);
console.log(result2);


